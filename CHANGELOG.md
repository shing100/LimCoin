# 변경 기록

무엇이 **왜** 바뀌었는지는 [docs/HISTORY.md](docs/HISTORY.md) 에 길게 적혀
있다. 이 파일은 짧은 목록이다.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 를 따르고,
판번호는 [유의적 버전](https://semver.org/lang/ko/)을 따른다. 다만 이 프로젝트
에서는 **합의 규칙이 바뀌면 major** 다 — 그것은 하드포크이고, 안 바꾼 노드와
체인이 갈라진다.

## [미출시]

### 추가

- **비트코인 호환 JSON-RPC** (`POST /rpc`). 메서드 29개, 이름·응답 모양·오류
  코드가 비트코인 코어와 같다. 배치와 named params 를 받는다. 거래소·결제
  도구를 새로 짜지 않고 붙일 수 있다.
- **raw 트랜잭션·블록 hex** (SPEC 3.3). 해제 데이터까지 담는 전송용 형식.
  기존 직렬화(txid 미리보기)와 독립이다 — 서명이 달라져도 txid 는 같다.
- **퍼징·불변식 테스트** (`test/fuzz.test.js`). 씨앗 고정 난수로 "어떤 입력이
  와도 성립해야 하는 것"만 본다. `LIMCOIN_FUZZ_SEED` 로 씨앗을 바꾼다.
- **합의 테스트 벡터** (`docs/vectors.json`, `scripts/vectors.js`). 다른 언어
  구현이 맞춰 볼 값이자, 리팩터링이 규칙을 바꿨는지 잡는 그물.
- **릴리스 산출물** (`scripts/release.js`). tar.gz + SHA256SUMS. 같은 커밋
  에서 두 번 돌리면 같은 해시가 나온다(재현 가능).
- **린트·커버리지·CI**. `yarn lint` / `yarn coverage` / `yarn fuzz`.
  GitHub Actions 가 Node 20·22·24 에서 돌리고 컨테이너까지 띄워 본다.
- SECURITY.md, CONTRIBUTING.md, 이 파일.

### 고침

- **`chainwork` 를 물을 때마다 체인을 통째로 훑던 것.** 높이별 누적값을 캐시
  한다. 10만 블록에서 31.57ms → 0.000014ms, 그리고 이제 높이에 비례하지
  않는다. `getblock`·`getblockchaininfo` 뿐 아니라 P2P 방송, `/info`,
  `/metrics` 가 다 이 값을 쓰고 있었다.
- **`listunspent` 가 `maxconf` 와 `addresses` 를 버리던 것.** 주소 하나만
  달라는 요청에 지갑 전체를 돌려주고 있었다.
- **`walletpassphrase` 가 `timeout` 을 버리던 것.** 부른 쪽은 잠긴 줄 알지만
  지갑은 계속 열려 있었다.
- **`getwalletinfo`** 의 `txcount`(노드 전체 주소 수를 주고 있었다)와
  `unconfirmed_balance`(0 고정).
- `getblock` 이 블록을 세 번 직렬화하던 것, MTP 에 앞부분 전체를 넘기던 것,
  `bits` 가 8자리로 채워지지 않던 것.
- **raw 왕복이 깨지는 자리** (퍼저가 찾음). 주소 바이트가 UTF-8 이 아니면
  `toString("utf8")` 이 U+FFFD 로 바꿔 1바이트가 3바이트가 됐고, 서로 다른
  바이트열이 같은 트랜잭션으로 읽혔다. 이제 거부한다.
- **자동 채굴의 경합** (린트가 찾음). `stop()` 이 기다리는 사이 `start()` 가
  불리면 돌아온 `stop()` 이 새 루프를 지워, 다음 `stop()` 이 아무것도
  기다리지 않았다.
- `getrawtransaction` 이 `findTx` 의 반환 모양을 잘못 읽던 것.

### 필요 사양

- Node **20** 이상 (전에는 18). 선택적 catch 바인딩을 쓴다.

## 1.0.0 이전

판번호를 붙이기 전의 작업이다. PR 단위로 적는다.

- **#17** 되감기 값을 깊이에 맞추고 체크포인트 구멍을 막음. reorg 비용을
  갈라진 부분만큼으로(디스크 읽기 121회 → 1회).
- **#16** 블록 본문을 디스크로(헤더만 메모리, 700블록 12.2MB → 9.3MB).
  피어 사이 전송 암호화 — X25519 + ChaCha20-Poly1305, Ed25519 노드 신원과 고정.
- **#15** 스크립트(P2SH 다중서명·CLTV 타임락·HTLC), 바이트 기준 수수료와
  CPFP·RBF, chainstate 스냅샷(시작 388ms → 45ms), 되감기 상한·체크포인트,
  피어 점수와 밴, 지갑 파일 암호화(scrypt + AES-256-GCM).
- **#14** 난이도를 256비트 목표값(`bits`)과 LWMA 로. 블록마다 조정, 헤더에
  `version`, regtest 망 추가. **합의 변경.**
- **#13** 난이도 창과 테스트넷 최소 난이도, 채굴 템플릿 갱신, 워커 풀,
  mempool 이어 쓰기, 단일 tx 브로드캐스트.
- **#12** 거래소가 받아 돌릴 수 있는 노드 — 정규 직렬화, Base58Check 주소,
  암호를 Node 내장 `crypto` 로(low-S), 테스트넷, raw tx, Docker, SPEC·EXCHANGE
  문서. **합의 변경.**
- **#11** 피어 발견(addr 교환), 권장 수수료, 헤더 동기화 메모리 절감.
- **#10** 헤더 우선 동기화, 잔돈 없는 코인 선택(BnB)과 dust, 피어 재연결.
- **#9** 체인 동기화를 조각으로(locator), `POST /peers` 인증, mempool 수시 저장.
- **#8** P2P 메시지 한 줄로 노드가 죽던 것, 코인베이스 성숙도, 타임스탬프
  규칙(MTP + 2시간), mempool 저장.
- **#7** reorg undo 데이터, 작업증명 검증(그 전에는 안 하고 있었다),
  조회 색인, 지갑 이어 보내기.
- **#6** 다중 코어 채굴, 한 블록 안에서 이어 쓰기, reorg 비용 절감.
- **#5** BIP39 니모닉, worker_threads 채굴.
- **#3** BIP32 HD 지갑(백서 10장), 주소별 색인.
- **#2** 체인 영속성, 지갑 API 인증, 자동 채굴, UTXO 색인.
- **#1** 2018년 코드의 버그 수정 — Linux 에서 뜨지 않던 대소문자 불일치,
  죽은 코드였던 서명 검증, 커밋되어 있던 제네시스 개인키. 백서 6·7·8장 반영.
