# 보안

## 먼저 알아야 할 것

**이 체인에는 아직 공개 해시레이트가 없다.** 지금 난이도는 노트북 한 대로
체인을 다시 쓸 수 있는 수준이다. 코드가 갖춰졌다는 것과 망이 안전하다는 것은
다른 이야기다. 아래의 어떤 대책도 해시레이트의 대체물이 아니다.

**외부 보안 감사를 받지 않았다.** 구현은 하나뿐이고(Node.js), 그래서 합의
버그가 곧 체인 전체의 버그다.

큰 금액을 다루는 곳이라면 이 두 줄을 먼저 읽고 판단하기 바란다.

## 취약점 알리기

공개 이슈로 올리지 말 것. 고쳐지기 전에 알려지면 쓰고 있는 사람이 위험해진다.

- GitHub 의 **Security → Report a vulnerability**
  (<https://github.com/shing100/LimCoin/security/advisories/new>)
- 그게 막혀 있으면 리포 주인에게 직접

같이 보내 주면 좋은 것:

- 어떤 규칙이 어떻게 깨지는지 (되도록 재현 절차나 테스트)
- 영향 범위 — 노드가 죽는가, 돈이 사라지는가, 체인이 갈라지는가
- 어느 판(커밋)에서 봤는지, 어느 망(mainnet/testnet/regtest)인지

받으면 **72시간 안에** 받았다는 답을 보낸다. 고치는 데 걸리는 시간은 사안에
따라 다르지만, 진행 상황은 알린다. 원하면 수정 공지에 이름을 적는다.

## 어느 것이 취약점인가

이런 것은 보고해 주기 바란다.

| 갈래 | 예 |
|---|---|
| 합의 | 유효하지 않은 블록·트랜잭션이 받아들여진다. 유효한 것이 거부된다. 구현마다 답이 갈린다 |
| 발행 | 보조금·수수료 규칙을 벗어난 코인이 생긴다. 같은 출력을 두 번 쓴다 |
| 원격 종료 | 피어가 보낸 한 줄로 노드가 죽거나, 메모리·디스크가 무한히 는다 |
| 키 | 지갑 파일, 로그, API 응답으로 개인키나 씨앗이 샌다 |
| 인증 | 지갑 토큰 없이 지갑 엔드포인트가 돈다. 토큰을 우회한다 |
| 격리 | `LIMCOIN_WALLET=off` 인데 키가 관여하는 동작이 있다 |

이런 것은 이미 알고 있는 한계다 — 보고보다 개선 제안이 낫다.

- 51% 공격, 셀피시 마이닝 등 해시레이트에 달린 것 (위 첫 줄)
- 채굴 보상이 걸린 경합(같은 높이 블록 둘)에서 하나가 밀리는 것 — 정상이다
- 공개 API(`/blocks`, `/transactions` 등)를 아무나 읽을 수 있는 것 — 의도다
- `POST /transactions/raw` 에 인증이 없는 것 — 유효한 서명이 곧 권한이고,
  P2P 로도 되는 일이다
- 노드를 열어 두고 방화벽 없이 돌리는 운영상의 선택

## 운영자가 할 수 있는 것

| | |
|---|---|
| 키를 노드에 두지 않는다 | `LIMCOIN_WALLET=off`. 프로세스가 뚫려도 가져갈 것이 없다 |
| 지갑을 쓴다면 잠근다 | `POST /me/passphrase` (scrypt + AES-256-GCM). 잠긴 동안 지갑 엔드포인트는 `423` |
| 토큰을 고정한다 | `LIMCOIN_WALLET_TOKEN`. `none` 은 로컬 실습에서만 |
| 지갑 엔드포인트를 밖에 열지 않는다 | 리버스 프록시에서 `/me/*`, `POST /blocks`, `/peers` 를 막는다 |
| 피어 사이를 암호화한다 | `LIMCOIN_ENCRYPT=required`. 아는 피어는 `ws://host:port#<nodeid>` 로 고정한다 |
| 되감기 상한 | `LIMCOIN_MAX_REORG_DEPTH`(기본 100). 깊은 재구성을 무게와 무관하게 거부한다 |
| 받은 것을 확인한다 | `dist/SHA256SUMS` (`scripts/release.js` 가 만든다. 같은 커밋이면 해시도 같다) |
| 감시한다 | `limcoin_tip_age_seconds` 가 300 을 넘거나 `limcoin_peers` 가 0 이면 경보 |

## 이미 한 것

무엇을 왜 넣었는지는 [docs/HISTORY.md](docs/HISTORY.md) 에 자세히 있다. 요약하면 —

- 서명은 secp256k1 DER **low-S** 만 받는다(malleability). txid 는 해제
  데이터를 덮지 않는다.
- 작업증명·타임스탬프(MTP + 2시간)·코인베이스 성숙도·블록 크기·중복 txid
  (CVE-2012-2459)를 전부 검증한다.
- P2P: 망 매직 확인, 피어 점수와 밴(24시간), 메시지 속도 제한,
  X25519 + ChaCha20-Poly1305 전송 암호화와 Ed25519 노드 신원.
- mempool: 바이트 한도와 퇴출, 최소 수수료율, RBF 규칙.
- 저장: chainstate 스냅샷, undo 데이터, 체크포인트 아래로는 갈라지지 않는다.
- 남이 보낸 것을 다루는 자리는 퍼저로 두들긴다(`test/fuzz.test.js`).
- 합의는 테스트 벡터로 고정한다(`docs/vectors.json`) — 리팩터링이 규칙을
  바꾸면 CI 가 잡는다.
